# 코드 점검과 실행 조건

## 확인 결과

API를 사용하지 않는 테스트 46개를 통과했습니다. 실제 LangGraph를 실행해 정상 종료와 최대 재시도 후 검토 대기 전환을 확인하고, CSV 파일 입출력으로 13건을 10건 단위로 처리해 마지막 3건까지 저장되는지 검사했습니다.

| 확인한 문제 | 수정 내용 |
| --- | --- |
| 파싱된 값이 목록·숫자일 때 후속 처리 실패 | JSON 객체 여부 및 필수 필드·타입 검사 |
| Critic 표식 누락 시 파싱 예외 | 수정 필요 상태로 전환 |
| 잘못된 HITL 입력도 승인으로 종료 | 전체 명령 검증 후에만 승인, 오류 시 재입력·보류 |
| 얕은 복사로 원본 항목까지 변경 | 독립 복사본에 명령 전체를 적용한 뒤 확정 |
| 마지막 배치가 기준 건수보다 작으면 누락 | 남은 건수만큼 처리 |
| 실행 전 기존 분석 결과 초기화 | 원본 보존, 별도 결과 CSV 사용 |
| 배치 처리 중 콘솔 입력 대기 | `needs_review` 상태 저장, 대화형 검토와 분리 |
| 대시보드의 감성 비율을 리뷰 비율로 표시 | 속성별 판정 수 기준으로 명칭 수정 |
| 현재 조회 시각을 분석 시각으로 표시 | 실제 저장된 처리 시각 표시, 없으면 빈 값 |
| 긍정·부정 범례가 데이터 순서에 따라 뒤바뀔 수 있음 | 감성 순서와 범례 명시 |
| 키 일부와 실행 출력이 공개 노트북에 저장 | 키 값 출력 제거, 저장된 실행 출력 정리 |

## 테스트 조건

- 점검일: 2026-09-10
- 입력: 테스트 내부의 가상 리뷰·응답과 임시 CSV
- 외부 LLM: 모의 응답으로 대체, 유료 API 호출 없음
- 테스트 대상: 노트북 함수, 실제 LangGraph 상태 전달·분기, 파일 저장, 대시보드 데이터 변환 함수
- 실행: `python -m pytest test_review_agent.py -q`
- 원본 노트북 기준 Git blob: `4c58ff42767f7e0d8c9934637b140a4657a15d8b`

원본 노트북은 키 출력과 저장된 출력만 정리해 보존했습니다. 현재 실행에는 `review_agent.ipynb`를 사용합니다. 과거 Git 이력까지 삭제하거나 재작성하지 않았으므로, 기존 키가 유효하다면 키 교체가 필요합니다.

## 팀 결과보고서 수치의 의미

보고서에는 `data.csv`의 50개 샘플 평가에서 format 0.64→1.00, 토큰 93.5K→65.7K로 기재되어 있습니다. 원본 평가 함수는 구조·필드·타입·중복·Critic 판정 형식을 검사하며, 실제 감성 정답과 비교하지 않습니다. 빈 items나 Revise 판정도 다른 형식 조건을 충족하면 점수 1이 될 수 있습니다.

따라서 포트폴리오에는 **출력 형식 평가**로 기재합니다. 이번 테스트 결과로 해당 과거 수치를 다시 측정했다고 주장하지 않습니다. 원본 리뷰 데이터와 LangSmith 평가 데이터셋은 제공된 저장소에 없어 새로운 성능 비교를 수행하지 않았습니다.

## 남은 확인 범위

실제 OpenAI 응답 품질, LangSmith 평가 재실행, Streamlit 화면 전체 조작은 이번 오프라인 검사에 포함하지 않았습니다. 사람 검토가 필요한 배치 결과는 자동 승인하지 않고 대기 상태로 보존하며, 웹 검수·재개·저장 과정은 후속 개선 대상입니다.

공개한 `demo_results.csv`는 대시보드 확인용 수기 예시이며 LLM 실행 결과가 아닙니다.
